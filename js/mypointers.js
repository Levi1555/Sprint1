//interval:
//gGameInterval = setInterval(play, GAME_FREQ); //start interval for the function "play" in every GAME_FREQ milisecond
//clearInterval(gGameInterval) //stop interval

//array
//array.shift() //מוציא את האיבר הראשון
//array.unshift(x)  //מכניס איבר איקס לערך הראשון
//array.push(x) מכניס איבר איקס לערך אחרון
//array.pop()  מוציא איבר איקס מהערך האחרון
//array.splice(x,y) מוציא ווי ערכים מהמערך החל מערך במיקום איקס
//array.splice(x,0,z) מכניס את זד לאחר מיקום איקס
//array.splice(x,y,z) מכניס את זד לאחר מיקום איקס ומוציא ווי ערכים במקום
//string.split('x') מפצל מחרוזת למערך לפי מפריד איקס
//array.join('x') מאחד מערך והופך למחרוזת לפי מפריד איקס
//array.slice(x,y) משכפל מערך ממקום איקס עד מקום ווי. אם לא שמים כלום הוא משכפל את הכל
//array.includes(x) בודק אם במערך יש ערך איקס
//array.inexOf(x)בודק את המיקום הראשון של איקס במערך, אם לא קיים מחזיר  מינוס אחד 

//string.indexOf(x)  מחזיר את מיקום וי במחרוזת 
//
